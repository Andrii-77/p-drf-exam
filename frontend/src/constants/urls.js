const baseURL = '/api'

const auth = '/auth'
const cars = '/cars'
const users = '/users'

const urls = {
    auth: {
        login: auth,
    },
    cars,
    users
}

export {
    baseURL,
    urls
}